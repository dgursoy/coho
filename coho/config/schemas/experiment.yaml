# config/schemas/experiment.yaml

experiment:
  type: dict
  required: true
  schema:
    id:
      type: string
      required: true
    model:
      type: string
      required: true
    properties:
      type: dict
      required: false
      default: {}
      schema:
        components:
          type: list
          required: true
